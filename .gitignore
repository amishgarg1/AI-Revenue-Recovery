# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Secrets — never commit real keys, test-mode or otherwise
.env
.env.local

# Node / Next.js
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Editors / OS / local tooling
.vscode/
.idea/
.DS_Store
Thumbs.db

# `next dev` regenerates this on every start; it is local scaffolding, not
# part of the project.
frontend/AGENTS.md

# NOT ignored, on purpose:
#   backend/demo.db  - the committed, pre-seeded database. Judges clone the
#                      repo and get the exact numbers in EVALUATION.md without
#                      running the generator or needing any API key.
#   docs/results.json - machine-readable results, regenerated by `make report`.
